# msf-reasoning-agent — Project Instructions

## Branch Policy

**Always work on `main`.** This project uses a single trunk branch. Do not create feature branches. Commit directly to `main` and push.

If you find yourself on any branch other than `main`, switch back immediately:
```bash
git checkout main
```
