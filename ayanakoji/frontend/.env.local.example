# Copy to .env.local and adjust. NEXT_PUBLIC_* values are exposed to the browser
# (public config only — never put secrets here).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Backend-only (NOT NEXT_PUBLIC — never exposed to the browser): Foundry IQ KB MCP endpoint
KNOWLEDGE_BASE_MCP_ENDPOINT=https://athenaeum-search-759d4908.search.windows.net/knowledgebases/athenaeum-knowledge-base/mcp?api-version=2025-08-01-preview
